{{#extend "default-layout"}}

    {{#prepend "title"}}{{blogPost.title}} - {{/prepend}}

    {{#append "head"}}
        <link href="/css/default-blog-styles.css" rel="stylesheet"/>
        <link href="/css/default-block-styles.css" rel="stylesheet"/>
    {{/append}}

    {{#replace "content"}}

        <div class="body-content">
            <div class="container">

                <div class="row">

                    <h1 class="blog-post-header">{{blogPost.title}}</h1>

                    <div class="blog-post-info">
                        Posted by {{memberLink blogPost.author}}
                    </div>

                    {{#if blogPost.featureImageUrl}}
                        <div class="feature-image">
                            <img src="{{blogPost.featureImageUrl}}"/>
                        </div>
                    {{/if}}

                    <div class="blog-post-content">
                        {{#with blogPost.content}}
                            {{> content/content}}
                        {{/with}}
                    </div>

                </div>
            </div>
        </div>

    {{/replace}}

{{/extend}}

